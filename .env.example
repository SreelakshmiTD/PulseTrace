# PulseTrace Environment Configuration

# Optional: Gemini API Key (used only for explanation polish)
GOOGLE_API_KEY=your_api_key_here

# Optional: Cloud Deployment (only needed if deploying to Vertex AI)
PROJECT_ID=your-gcp-project-id
REGION=us-central1   # or any preferred region
